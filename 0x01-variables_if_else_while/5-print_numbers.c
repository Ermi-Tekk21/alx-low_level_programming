#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
	{
	int single_digit;

	for (single_digit = 0; single_digit < 10; single_digit++)
	{
		printf("%d", single_digit);
	}
	printf("\n");

	return (0);

	}
