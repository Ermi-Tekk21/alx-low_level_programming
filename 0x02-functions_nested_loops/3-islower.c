#include "main.h"

/**
 * _islower - check upppercase and lowercase
 *
 * @c: char parameter from the user
 * Return: 0 if uppercase, 1 if lowercase.
 */


int _islower(int c)
{
	if (c >= 'a')
	{
		return (1);
	}
	return (0);
}
